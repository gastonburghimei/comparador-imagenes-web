# Plantilla de variables de entorno para BigQuery
# Copia este archivo como .env y completa los valores

# Google Cloud Project ID
GOOGLE_CLOUD_PROJECT=tu-proyecto-bigquery

# Ruta al archivo JSON de credenciales de service account (opcional)
# Si no está configurado, se usarán las credenciales por defecto (ADC)
GOOGLE_APPLICATION_CREDENTIALS=ruta/a/tu/service-account-key.json

# Ubicación por defecto para BigQuery (US, EU, etc.)
BIGQUERY_LOCATION=US

# Dataset por defecto (opcional)
BIGQUERY_DEFAULT_DATASET=tu_dataset_por_defecto

# Configuración adicional para desarrollo
DEBUG=True
FLASK_ENV=development 